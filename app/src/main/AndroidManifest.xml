<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lebelleami.recipe">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controllers.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controllers.RecipesListActivity"
            android:theme="@style/Base.AppThemeTwo"/>
        <activity
            android:name=".Controllers.RecipesDetailsActivity"
            android:label="@string/title_activity_recipes_details"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".Controllers.StepsActivity"
            android:theme="@style/Base.AppThemeTwo">
        </activity>
    </application>

</manifest>